# clipboard-manager

## Setup

```bash
uv sync
```

## Usage

```bash
just run
```

## Development

```bash
just check   # lint + format check + type check
just test    # run tests
just fmt     # auto-format
```
